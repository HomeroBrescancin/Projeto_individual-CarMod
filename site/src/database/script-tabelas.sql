create database carmod;
use carmod;

CREATE TABLE usuario (
    id INT PRIMARY KEY auto_increment,
    Nome VARCHAR(45),
    email VARCHAR(45),
     senha VARCHAR(45),
    Estado VARCHAR(45)
);

select * from usuario;








